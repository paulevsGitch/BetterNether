package org.betterx.betternether.world.features;

import net.minecraft.world.level.levelgen.feature.configurations.PointedDripstoneConfiguration;

public class ClusterFeatureConfig extends PointedDripstoneConfiguration {
    public ClusterFeatureConfig(float f, float g, float h, float i) {
        super(f, g, h, i);
    }
}
